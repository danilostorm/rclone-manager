--- a/app/media_pools.py
+++ b/app/media_pools.py
@@ -1,3 +1,4 @@
+import errno
 import json
 import os
 import signal
@@ -54,26 +55,80 @@
     def mount_path(slug):
         return MOUNT_ROOT / "_media-pools" / slug
 
+    STALE_MOUNT_ERRNOS = {errno.ENOTCONN, errno.EIO, getattr(errno, "ESTALE", 116)}
+
+    @staticmethod
+    def _raw_mountpoint(path):
+        path = str(path)
+        try:
+            return subprocess.run(
+                ["mountpoint", "-q", path],
+                stdout=subprocess.DEVNULL,
+                stderr=subprocess.DEVNULL,
+                timeout=5,
+            ).returncode == 0
+        except Exception:
+            try:
+                return os.path.ismount(path)
+            except OSError:
+                return False
+
+    @classmethod
+    def _mount_access_error(cls, path):
+        """Return the OSError for a dead FUSE endpoint, otherwise None.
+
+        A stale FUSE mount can still be reported by mountpoint(1) even though
+        every filesystem access fails with ENOTCONN (Errno 107).  Checking the
+        mount root with stat(2) lets the Manager distinguish a healthy mount
+        from a detached transport endpoint without walking the remote tree.
+        """
+        if not cls._raw_mountpoint(path):
+            return None
+        try:
+            os.stat(path)
+            return None
+        except OSError as exc:
+            if exc.errno in cls.STALE_MOUNT_ERRNOS:
+                return exc
+            return None
+
+    @classmethod
+    def _broken_mount(cls, slug):
+        return cls._mount_access_error(cls.mount_path(slug)) is not None
+
+    @classmethod
+    def is_mounted(cls, slug):
+        path = cls.mount_path(slug)
+        return cls._raw_mountpoint(path) and cls._mount_access_error(path) is None
+
     @classmethod
     def ensure_dirs(cls, slug):
-        for p in (cls.pool_root(slug), cls.cache_dir(slug), cls.mount_path(slug)):
-            p.mkdir(parents=True, exist_ok=True)
+        # Never mkdir/stat through a dead FUSE endpoint.  Clean it first, then
+        # recreate the plain directory used as the next mount target.
+        cls.pool_root(slug).mkdir(parents=True, exist_ok=True)
+        cls.cache_dir(slug).mkdir(parents=True, exist_ok=True)
+        mount = cls.mount_path(slug)
+        mount.parent.mkdir(parents=True, exist_ok=True)
+        try:
+            mount.mkdir(parents=True, exist_ok=True)
+        except OSError as exc:
+            if exc.errno not in cls.STALE_MOUNT_ERRNOS:
+                raise
+            cls._force_unmount_path(mount)
+            mount.mkdir(parents=True, exist_ok=True)
         try:
             cls.pool_root(slug).chmod(0o700)
         except OSError:
             pass
 
     @staticmethod
-    def _mounted_at(path):
+    def _force_unmount_path(path):
         path = str(path)
-        try:
-            return subprocess.run(["mountpoint", "-q", path], timeout=5).returncode == 0
-        except Exception:
-            return os.path.ismount(path)
-
-    @classmethod
-    def is_mounted(cls, slug):
-        return cls._mounted_at(cls.mount_path(slug))
+        for cmd in (["fusermount3", "-uz", path], ["fusermount", "-uz", path], ["umount", "-l", path]):
+            try:
+                subprocess.run(cmd, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL, timeout=8)
+            except Exception:
+                pass
 
     @staticmethod
     def _safe_category(value):
@@ -205,6 +260,19 @@
             members = [m for m in list_media_pool_members(slug) if m.get("enabled")]
             if not members:
                 raise RuntimeError("Adicione pelo menos um Drive ao Media Pool")
+
+            if self._broken_mount(slug):
+                stale_proc = self.processes.pop(slug, None)
+                if stale_proc and stale_proc.poll() is None:
+                    try:
+                        os.killpg(os.getpgid(stale_proc.pid), signal.SIGTERM)
+                        stale_proc.wait(timeout=5)
+                    except Exception:
+                        try:
+                            os.killpg(os.getpgid(stale_proc.pid), signal.SIGKILL)
+                        except Exception:
+                            pass
+                self._cleanup_stale_mount(slug, force=True)
             self.ensure_dirs(slug)
             member_errors = self._ensure_members(slug, members)
             # Give account mounts a short chance to become visible. MountManager.start
@@ -249,15 +317,17 @@
         update_media_pool(slug, status="mounted" if self.is_mounted(slug) else "starting", last_error="")
         return True
 
-    def _cleanup_stale_mount(self, slug):
-        path = str(self.mount_path(slug))
-        if self.is_mounted(slug):
+    def _cleanup_stale_mount(self, slug, force=False):
+        path = self.mount_path(slug)
+        if not force and self.is_mounted(slug):
             return
-        for cmd in (["fusermount3", "-uz", path], ["umount", "-l", path]):
-            try:
-                subprocess.run(cmd, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL, timeout=5)
-            except Exception:
-                pass
+        self._force_unmount_path(path)
+        # The detached FUSE entry may disappear asynchronously after -uz/-l.
+        # Retry a few times before recreating/using the mount directory.
+        for _ in range(10):
+            if not self._raw_mountpoint(path):
+                break
+            time.sleep(0.1)
 
     def stop(self, slug, disable=True):
         with self.lock:
@@ -271,14 +341,8 @@
                         os.killpg(os.getpgid(proc.pid), signal.SIGKILL)
                     except Exception:
                         pass
-            path = str(self.mount_path(slug))
-            for cmd in (["fusermount3", "-uz", path], ["umount", "-l", path]):
-                try:
-                    subprocess.run(cmd, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL, timeout=8)
-                except Exception:
-                    pass
-                if not self.is_mounted(slug):
-                    break
+            path = self.mount_path(slug)
+            self._force_unmount_path(path)
             fields = {"status": "stopped", "last_error": ""}
             if disable:
                 fields["enabled"] = 0
@@ -295,6 +359,12 @@
         slug = pool["slug"]
         members = [m for m in list_media_pool_members(slug) if m.get("enabled")]
         missing = sorted({m["account_slug"] for m in members if not self.account_mounts.is_mounted(m["account_slug"])})
+        broken = self._mount_access_error(self.mount_path(slug))
+        if broken is not None:
+            msg = f"Mount FUSE desconectado ({broken}); recuperação automática pendente"
+            if pool.get("status") != "error" or pool.get("last_error") != msg:
+                update_media_pool(slug, status="error", last_error=msg)
+            return "error"
         if self.is_mounted(slug):
             if missing:
                 msg = "Drives indisponíveis: " + ", ".join(missing)
--- a/app/mounts.py
+++ b/app/mounts.py
@@ -1,3 +1,4 @@
+import errno
 import os
 import signal
 import subprocess
@@ -28,24 +29,60 @@
             except Exception:
                 pass
 
+    STALE_MOUNT_ERRNOS = {errno.ENOTCONN, errno.EIO, getattr(errno, "ESTALE", 116)}
+
     @staticmethod
-    def is_mounted(slug):
-        path = str(mount_path(slug))
+    def _raw_mountpoint(path):
+        path = str(path)
         try:
-            return subprocess.run(["mountpoint", "-q", path], timeout=5).returncode == 0
+            return subprocess.run(
+                ["mountpoint", "-q", path],
+                stdout=subprocess.DEVNULL,
+                stderr=subprocess.DEVNULL,
+                timeout=5,
+            ).returncode == 0
         except Exception:
-            return os.path.ismount(path)
+            try:
+                return os.path.ismount(path)
+            except OSError:
+                return False
+
+    @classmethod
+    def _mount_access_error(cls, path):
+        if not cls._raw_mountpoint(path):
+            return None
+        try:
+            os.stat(path)
+            return None
+        except OSError as exc:
+            if exc.errno in cls.STALE_MOUNT_ERRNOS:
+                return exc
+            return None
+
+    @classmethod
+    def is_mounted(cls, slug):
+        path = mount_path(slug)
+        return cls._raw_mountpoint(path) and cls._mount_access_error(path) is None
 
-    def _cleanup_stale_mount(self, slug):
-        path = str(mount_path(slug))
-        if self.is_mounted(slug):
-            return
-        for cmd in (["fusermount3", "-uz", path], ["umount", "-l", path]):
+    @staticmethod
+    def _force_unmount_path(path):
+        path = str(path)
+        for cmd in (["fusermount3", "-uz", path], ["fusermount", "-uz", path], ["umount", "-l", path]):
             try:
-                subprocess.run(cmd, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL, timeout=5)
+                subprocess.run(cmd, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL, timeout=8)
             except Exception:
                 pass
 
+    def _cleanup_stale_mount(self, slug, force=False):
+        path = mount_path(slug)
+        if not force and self.is_mounted(slug):
+            return
+        self._force_unmount_path(path)
+        for _ in range(10):
+            if not self._raw_mountpoint(path):
+                break
+            time.sleep(0.1)
+
     def build_command(self, account):
         slug = account["slug"]
         remote = f"{slug}:"
@@ -84,6 +121,18 @@
             account = get_account(slug)
             if not account:
                 raise RuntimeError("Conta não encontrada")
+            if self._mount_access_error(mount_path(slug)) is not None:
+                stale_proc = self.processes.pop(slug, None)
+                if stale_proc and stale_proc.poll() is None:
+                    try:
+                        os.killpg(os.getpgid(stale_proc.pid), signal.SIGTERM)
+                        stale_proc.wait(timeout=5)
+                    except Exception:
+                        try:
+                            os.killpg(os.getpgid(stale_proc.pid), signal.SIGKILL)
+                        except Exception:
+                            pass
+                self._cleanup_stale_mount(slug, force=True)
             ensure_account_dirs(slug)
             if not config_path(slug).exists():
                 raise RuntimeError("Configuração OAuth/rclone.conf não encontrada")
@@ -142,14 +191,7 @@
                     except Exception:
                         pass
 
-            path = str(mount_path(slug))
-            for cmd in (["fusermount3", "-uz", path], ["umount", "-l", path]):
-                try:
-                    subprocess.run(cmd, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL, timeout=8)
-                except Exception:
-                    pass
-                if not self.is_mounted(slug):
-                    break
+            self._force_unmount_path(mount_path(slug))
 
             fields = {"status": "stopped", "last_error": ""}
             if disable:
@@ -165,8 +207,12 @@
 
     def refresh_status(self, account):
         slug = account["slug"]
+        broken = self._mount_access_error(mount_path(slug))
         mounted = self.is_mounted(slug)
         proc = self.processes.get(slug)
+        if broken is not None:
+            update_account(slug, status="error", last_error=f"mount FUSE desconectado: {broken}")
+            return "error"
         if mounted:
             if account["status"] != "mounted":
                 update_account(slug, status="mounted", last_error="")
@@ -185,7 +231,12 @@
         while not self._stop_event.wait(8):
             try:
                 for account in list_accounts():
-                    self.refresh_status(account)
+                    status = self.refresh_status(account)
+                    if account.get("enabled") and status in {"error", "stopped"}:
+                        try:
+                            self.start(account["slug"], enable=False)
+                        except Exception as exc:
+                            update_account(account["slug"], status="error", last_error=str(exc)[-1200:])
             except Exception:
                 pass
 
--- a/app/app.py
+++ b/app/app.py
@@ -143,7 +143,7 @@
 ADMIN_USER = os.environ.get("ADMIN_USER", "admin")
 ADMIN_PASSWORD = os.environ.get("ADMIN_PASSWORD", "changeme")
 APP_SECRET = os.environ.get("APP_SECRET", secrets.token_hex(32))
-APP_VERSION = "1.3.0-rc1"
+APP_VERSION = "1.3.0-rc2"
 PLATFORM_NAME = os.environ.get("PLATFORM_NAME", "VPS Linux / Oracle Cloud")
 PLATFORM_SUBTITLE = os.environ.get("PLATFORM_SUBTITLE", "VPS Linux")
 
--- a/VERSION
+++ b/VERSION
@@ -1 +1 @@
-1.3.0-rc1
+1.3.0-rc2
--- a/Dockerfile
+++ b/Dockerfile
@@ -17,7 +17,7 @@
 COPY --from=rclonebin /usr/local/bin/rclone /usr/local/bin/rclone
 
 LABEL org.opencontainers.image.title="Rclone Manager VPS" \
-      org.opencontainers.image.version="1.3.0-rc1"
+      org.opencontainers.image.version="1.3.0-rc2"
 
 WORKDIR /app
 COPY requirements.txt /app/requirements.txt
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -3,7 +3,7 @@
     build:
       context: .
       dockerfile: Dockerfile
-    image: local/rclone-manager-vps:1.3.0-rc1
+    image: local/rclone-manager-vps:1.3.0-rc2
     container_name: rclone-manager-vps
     restart: unless-stopped
     init: true
--- a/install.sh
+++ b/install.sh
@@ -1,7 +1,7 @@
 #!/bin/bash
 set -euo pipefail
 
-VERSION="1.3.0-rc1"
+VERSION="1.3.0-rc2"
 APP_DIR="${APP_DIR:-/opt/rclone-manager}"
 MOUNT_ROOT="${MOUNT_ROOT:-/mnt/rclone-manager-remotes}"
 WEB_PORT="${WEB_PORT:-8787}"
--- a/README.md
+++ b/README.md
@@ -3,7 +3,7 @@
 
 ## Media Organizer / Smart Renamer
 
-Nova versão RC adiciona organização automática segura de bibliotecas para Plex/Jellyfin/Anime: scan somente leitura, prévia, score de confiança, revisão manual, TMDb opcional, AniDB/Anime-Lists, rename server-side e Undo. Veja [`RELEASE-1.3.0-rc1.md`](RELEASE-1.3.0-rc1.md).
+Nova versão RC adiciona organização automática segura de bibliotecas para Plex/Jellyfin/Anime: scan somente leitura, prévia, score de confiança, revisão manual, TMDb opcional, AniDB/Anime-Lists, rename server-side e Undo. Veja [`RELEASE-1.3.0-rc2.md`](RELEASE-1.3.0-rc2.md).
 
 Edição para VPS Linux (Ubuntu/Debian), preparada para instalação em **`/opt/rclone-manager`**.
