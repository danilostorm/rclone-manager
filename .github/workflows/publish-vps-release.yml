name: Legacy VPS release (disabled)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Releases separadas foram desativadas. Use o workflow Publish unified Rclone Manager release.'
